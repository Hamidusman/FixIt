import { Link } from "react-router-dom"

const GetStarted = () =>{
    return(
        <Link to='/booking' className="index-2 px-7 py-3 bg-accent text-secondary rounded-2xl text-[16px] md:text-base">Start Now</Link>
    )
}

export default GetStarted